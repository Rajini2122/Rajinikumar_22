write a c programme to print even numbers 1 to n
#include <stdio.h>

int main() {
   int i,n;
    printf("enter the value of n:");
    scanf("%d",&n);
    printf("even number from 1 to %d are:",n);
    for (i=1;i<=n;i++){
    if(i % 2==0){
    printf("%d ,",i);
     }
    }
    return 0;
}
